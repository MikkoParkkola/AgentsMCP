{
  "name": "agentsmcp",
  "private": true,
  "scripts": {
    "lint": "npx prettier -w . && npx eslint --fix .",
    "format": "npx prettier -w .",
    "test": "python -m pytest -q"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  }
}
